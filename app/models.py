from app import db

# Example of usage of a random User model
# class User(db.Model):
#     __tablename__ = '<tablename>'
#     id = db.Column(db.Integer, primary_key=True)
#     name = db.Column(db.String)
#     email = db.Column(db.String)
#     city_id = db.Column(db.Integer, db.ForeignKey('cities._id'))
